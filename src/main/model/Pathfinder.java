package model;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;

public class Pathfinder {

    // EFFECTS: finds the next position in the shortest path between two positions while keeping obstacles in mind
    public Position getNextPositionInShortestPath(Position source, Position target, HashSet<Position> obstacles) {
        return null; // stub
    }

    /* MODIFIES: this
     * EFFECTS: Returns a list of positions that represents the shortest path between two positions while navigating
     *          obstacles.
     */
    public List<Position> getListOfPositionsInShortestPath(Position source, Position target, HashSet<Position> obstacleSet) {
        return null; // stub
    }
}
