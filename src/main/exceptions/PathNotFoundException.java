package exceptions;

import java.io.IOException;

// An exception throw when a path to the target Node could not be found during A* pathfinding.
public class PathNotFoundException extends IOException {
}
