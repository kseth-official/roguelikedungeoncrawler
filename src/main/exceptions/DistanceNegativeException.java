package exceptions;

import java.io.IOException;

// An exception thrown when a negative value is provided for a distance measurement, which is illegal.
public class DistanceNegativeException extends IOException {
}
